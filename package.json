{
  "name": "todo-api",
  "version": "1.0.0",
  "scripts": {
    "start:api": "npx json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
